import { MinusCircleOutlined, PlusOutlined, PlusSquareOutlined } from "@ant-design/icons"
import { Button, Card, Col, Form, Input, Row, Select, Space, Table } from "antd"
import { useEffect, useState } from "react";
import { ColumnProps } from "antd/es/table";

const { Option } = Select;

export interface AttributeComponentProps {
    infoType: any;
}

export const AttributeComponent = (props: AttributeComponentProps) => {

   
    return (
        <>

           
        </>
    )
}
export default AttributeComponent